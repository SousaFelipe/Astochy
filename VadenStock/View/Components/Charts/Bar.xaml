<UserControl x:Class="VadenStock.View.Components.Charts.Bar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:VadenStock.View.Components.Charts"
             mc:Ignorable="d" 
             d:DataContext="{d:DesignInstance Type=local:Bar}"
             d:DesignHeight="6"
             d:DesignWidth="100">

    <Border x:Name="_BorderContent"
            VerticalAlignment="Stretch"
            HorizontalAlignment="Stretch"
            CornerRadius="{Binding BorderRadius}"
            Background="{Binding BackColor}">

        <Border x:Name="_Bar"
                CornerRadius="{Binding BorderRadius}"
                Width="{Binding CurrentWidth}"
                Height="{Binding CurrentHeight}"
                Background="{Binding BarColor}"/>

    </Border>
    
</UserControl>
